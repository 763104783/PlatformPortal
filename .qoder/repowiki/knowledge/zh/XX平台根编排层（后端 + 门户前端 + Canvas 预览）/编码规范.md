- 所有 REST 接口统一以 `/api` 为前缀，请求携带 `Authorization: {token}` 头，响应遵循 `{ code, message, data }` 三字段结构。
- 权限按角色控制：ADMIN 可增删改查，USER 仅公开读，认证相关端点在 `API.md` 中集中声明。
- Canvas 预览模块通过 importmap 将多品牌 `*/canvas/*` 别名映射到同一 SDK，避免重复打包。