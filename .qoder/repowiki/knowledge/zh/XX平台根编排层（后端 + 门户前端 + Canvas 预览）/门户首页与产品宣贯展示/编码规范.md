- 所有异步数据加载统一放在 onMounted 中执行，并使用 try/catch 包裹，失败时 console.error 输出而不中断页面渲染。
- 模板内导航一律通过 $router.push('/path') 或 $router.back() 完成，不引入 router-link。
- 页面采用统一的深色科技风布局：外层容器加 tech-bg 类，header 使用半透明背景+backdrop-filter，内容区以 glass-card/fade-in-up 装饰。
- 维度枚举（USER_ECOLOGY/PRODUCT_SYSTEM/MODEL_SYSTEM/DATA_SYSTEM/IP）在 Showcase.vue 与 ShowcaseDetail.vue 中以本地常量形式硬编码，保持前后端分类键一致。